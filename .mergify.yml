pull_request_rules:
  - name: Automatic Merge
    conditions:
      - status-success=build
      - "#approved-reviews-by=@Lock-The-Door"
      - conflict=false
    actions:
      merge:
        method: rebase
